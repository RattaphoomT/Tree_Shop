# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# node-waf configuration
.lock-wscript
build/Release

# Dependency directories (ห้ามเอาขึ้นเด็ดขาด มันเยอะมาก)
node_modules/
jspm_packages/

# TypeScript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables (สำคัญที่สุด! ใส่ตรงนี้)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Production Build (ที่เรา deploy ไป Firebase ไม่ต้องเอาขึ้น Git ก็ได้)
dist/
dist-ssr/