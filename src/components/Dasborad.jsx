import { Button, Box } from "@mui/material";
import Layout from "../Layout/Layout";

const Dashboard = () => {
  return (
    <Layout>
      <Box>
        <Box sx={{ mb: 3 }}>
          {/* contain */}
          <h1>Content</h1>
        </Box>
      </Box>
    </Layout>
  );
};

export default Dashboard;
